apiVersion: v1
kind: Secret
metadata:
  name: pulsecart-secrets
  namespace: pulsecart
type: Opaque
stringData:
  # Azure landing-zone tfvars currently disable PostgreSQL Flexible Server.
  # Replace this only after enabling Azure Postgres or pointing at another real database.
  DB_PASSWORD: MISSING_AZURE_POSTGRES_PASSWORD
