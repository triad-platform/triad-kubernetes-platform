apiVersion: v1
kind: ConfigMap
metadata:
  name: cert-manager-bootstrap-notes
  namespace: cert-manager
data:
  controller: "placeholder"
  next_step: "replace with cert-manager install manifests and issuer configuration"
