apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: observability-aws-secrets
  namespace: observability
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
